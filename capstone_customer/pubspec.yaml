name: capstone_customer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3
  flutter_swiper: ^1.1.6       # image slider
  cloud_firestore: ^0.14.0+2     # FireStore
  firebase_core: "0.5.0"       # Firebase
  firebase_storage: ^4.0.0     # FireStorage
  google_maps_flutter:
  qrscan: ^0.2.17              # QR Code Scanner
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/